package com.example.demo.repository;

import com.example.demo.models.Core;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CoreRepository extends JpaRepository<Core, Integer> {
}
